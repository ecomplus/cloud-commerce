---
import {
  type Props as UseSharedDataProps,
  useSharedData,
} from '@@sf/composables/use-shared-data';

export type Props = UseSharedDataProps

const { getInlineClientJS } = await useSharedData(Astro.props);
---

<script async is:inline set:html={getInlineClientJS()} />
